# Optional — set these to switch Katha Cards into "accounts mode" (Supabase
# magic-link auth + server-verified entitlement + content protection + sync).
# Leave them UNSET to run in the static/device model. See supabase/README.md.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
